<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product List</title>
    <link rel="stylesheet" type="text/css" href="stylebackup.css">
</head>
<body>
    
    <button onclick="location.href='/products2'" class="navigation-button">Tops</button>
    <button onclick="location.href='/products3'" class="navigation-button">Pants</button>

    <h1>Product List</h1>
    
    <div class="filter-sidebar-container">
        <nav class="sidebar">
            <ul>
                <li><a href="products">SEARCH</a></li>
                <li><a href="upload.html">NEW MOODBOARD</a></li>
                <li><a href="saved.html">SAVED MOODBOARD</a></li>
            </ul>
        </nav>
        <div id="filter-container">
            <form id="filter-form">
                <input type="text" name="search" placeholder="Search...">
                <select name="colour">
                    <option value="">All Colours</option>
                    <option value="red">red</option>
                    <!-- Add options for colours dynamically if needed -->
                </select>
                <input type="number" name="minPrice" placeholder="Min Price">
                <input type="number" name="maxPrice" placeholder="Max Price">
                <button type="submit">Filter</button>
            </form>
        </div>
        
       
    </div>

    <div id="product-list">
        <% products.forEach(product => { %>
            <div class="product-item" data-image-url="<%= product.img %>">
                <h3><%= product.title.replace('Go to product: ', '') %></h3>
                <a href="<%= product.urlpg %>" target="_blank">
                    <img src="<%= product.img %>" alt="<%= product.title %>">
                </a>
                <p>Pricing: <%= product.pricing %></p>
                <div class="color-list">
                    <% JSON.parse(product.colour).forEach(colour => { %>
                        <span class="color-item"><%= colour %></span>
                    <% }); %>
                </div>
                <div class="dropdown">
                    <button class="dropbtn">Add to New Moodboard</button>
                    <div class="dropdown-content">
                      <a href="#" class="add-to-new-moodboard" data-image-url="<%= product.img %>">Add</a>
                    </div>
                </div>
                
            </div>
          
        <% }); %>
      
    </div>
    
    
    

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function() {
            $('.dropbtn').click(function() {
                $(this).siblings('.dropdown-content').toggle();
            });

            // Close the dropdown if the user clicks outside of it
            $(document).click(function(event) {
                if (!$(event.target).closest('.dropdown').length) {
                    $('.dropdown-content').hide();
                }
            });

            // Add image to new moodboard
            $('.add-to-new-moodboard').click(function(event) {
                event.preventDefault();
                const imageUrl = $(this).data('image-url');
                let newMoodboardImages = JSON.parse(localStorage.getItem('newMoodboardImages')) || [];
                newMoodboardImages.push(imageUrl);
                localStorage.setItem('newMoodboardImages', JSON.stringify(newMoodboardImages));
                alert('Image added to new moodboard');
            });

            // Handle filter form submission
            $('#filter-form').submit(function(event) {
                event.preventDefault();
                const formData = $(this).serialize();
                $.ajax({
                    url: '/products?' + formData,
                    method: 'GET',
                    success: function(data) {
                        $('#product-list').html(data);
                    },
                    error: function(xhr, status, error) {
                        console.error('Error fetching filtered products:', error);
                    }
                });
            });
        });
    </script>
</body>
</html>


<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product List</title>
    <link rel="stylesheet" type="text/css" href="style.css">
</head>
<body>

    <a href="/products2" class="navigation-button">Tops</a>
    
    <h1>Product List</h1>
    <div id="product-list">
        <% products.forEach(product => { %>
            <div class="product-item" data-image-url="<%= product.img %>">
                <h3><%= product.title.replace('Go to product: ', '') %></h3>
                <a href="<%= product.urlpg %>" target="_blank">
                    <img src="<%= product.img %>" alt="<%= product.title %>">
                </a>
                <p>Pricing: <%= product.pricing %></p>
                <div class="color-list">
                    <% JSON.parse(product.colour).forEach(colour => { %>
                        <span class="color-item"><%= colour %></span>
                    <% }); %>
                </div>
                <div class="dropdown">
                    <button class="dropbtn">Add to New Moodboard</button>
                    <div class="dropdown-content">
                      <a href="#" class="add-to-new-moodboard" data-image-url="<%= product.img %>">Add</a>
                    </div>
                </div>
            </div>
        <% }); %>
    </div>
    
    <button onclick="location.href='upload'">Go to New Moodboard</button>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function() {
            $('.dropbtn').click(function() {
                $(this).siblings('.dropdown-content').toggle();
            });

            // Close the dropdown if the user clicks outside of it
            $(document).click(function(event) {
                if (!$(event.target).closest('.dropdown').length) {
                    $('.dropdown-content').hide();
                }
            });

            // Add image to new moodboard
            $('.add-to-new-moodboard').click(function(event) {
                event.preventDefault();
                const imageUrl = $(this).data('image-url');
                let newMoodboardImages = JSON.parse(localStorage.getItem('newMoodboardImages')) || [];
                newMoodboardImages.push(imageUrl);
                localStorage.setItem('newMoodboardImages', JSON.stringify(newMoodboardImages));
                alert('Image added to new moodboard');
            });
        });
    </script>
</body>
</html> -->
